How to use:
Drop csv file/s to the executable using file explorer.

What it does:
Add prefix to column names except the first two.
Prefix is folder name.
Output new csv files with _edit suffix for each files dropped.
Program opens in command prompt and closes itself. No user interface.